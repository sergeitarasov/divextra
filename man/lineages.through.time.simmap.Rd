% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineages-through-time-simmap.R
\name{lineages.through.time.simmap}
\alias{lineages.through.time.simmap}
\title{Focal-state lineage counts through time across stochastic maps}
\usage{
lineages.through.time.simmap(
  sims,
  times,
  states,
  tips = NULL,
  probs = c(0.025, 0.975),
  quantile.type = 1L
)
}
\arguments{
\item{sims}{One simulation, a list of simulations from
\code{\link[=make.simmap.classe.td]{make.simmap.classe.td()}}, or a list of focused results.}

\item{times}{Nonnegative finite ages before present, in tree units (e.g. Ma).}

\item{states}{Exact state labels to pool, e.g. \code{c("S", "A.S", "S.R")}.
Each lineage counts once if its current state belongs to this set.}

\item{tips}{Optional observed tip labels. The same focus selection is
applied independently to every simulation using \code{\link[=keep.lineage.simmap]{keep.lineage.simmap()}}.}

\item{probs}{Two increasing quantile probabilities, default 0.025 and 0.975.}

\item{quantile.type}{Quantile algorithm passed to \code{\link[stats:quantile]{stats::quantile()}}, an
integer from 1 to 9. Default 1 selects observed counts; 7 interpolates.}
}
\value{
A \code{classe_td_ltt} list containing \code{counts} (time by simulation),
\code{summary} (time, mean, median, lower, upper, n.maps), \code{segments} (focal-state
intervals with simulation IDs), \code{times}, \code{states}, \code{probs}, and \code{tips}.
}
\description{
Focal-state lineage counts through time across stochastic maps
}
\details{
Counts living branches occupying any focal state, regardless of eventual
survival or extinction. At exact events uses the younger-side convention:
daughters count at birth, extinct lineages do not count at death, and state
transitions use the new state. Present-day counts use terminal tip states.
Before a map's root counts are zero. All maps, including zero-count maps,
contribute to every summary. Inputs must use a common age scale and state
coding. Quantiles are pointwise empirical map-distribution intervals
(type 1 by default), not confidence intervals for the mean or simultaneous bands.
With fixed parameters they do not include parameter uncertainty. For the
ASR-weighted mapper they describe that simulation distribution, not an exact
ClaSSE posterior. No extra conditioning or rejection is performed here.
}
