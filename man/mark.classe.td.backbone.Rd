% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-on-tree-classe-td.R
\name{mark.classe.td.backbone}
\alias{mark.classe.td.backbone}
\title{Mark a reconstructed tree as an episodic ClaSSE simulation backbone}
\usage{
mark.classe.td.backbone(
  tree,
  node.states,
  tip.states,
  k,
  state.labels = as.character(seq_len(k)),
  stem.length = NULL,
  stem.state = NULL
)
}
\arguments{
\item{tree}{A rooted, bifurcating, ultrametric \code{phylo} object.}

\item{node.states}{Named states for all internal nodes, or a \code{k} by \code{Nnode}
matrix of reconstructed state probabilities. Matrix columns are retained as
soft endpoint constraints rather than reduced to maximum-probability states.}

\item{tip.states}{Named states for all tips.}

\item{k}{Number of ClaSSE states.}

\item{state.labels}{Optional state labels in model order.}

\item{stem.length}{Length of the stem lineage. By default, uses
\code{tree$root.edge} when present, otherwise zero.}

\item{stem.state}{State at the old end of the stem. By default, uses the
reconstructed crown-root state.}
}
\value{
An object of class \code{classe_td_backbone}.
}
\description{
Creates a stable representation of a supplied extant tree, its tip states,
and reconstructed ancestral-state probabilities. Internal-node states are
interpreted as states immediately before the observed cladogenetic event.
}
\details{
A vector of reconstructed node states is represented internally as
one-hot probabilities. If a probability matrix is supplied, each column is
normalized and retained. During simulation, arrival in state \code{i} at that
node is accepted with the reconstructed probability for state \code{i}; a
rejected arrival triggers a local retry. Thus uncertain reconstructions do
not impose a single maximum-probability endpoint on every branch.
}
