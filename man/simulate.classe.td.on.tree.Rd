% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-on-tree-classe-td.R
\name{simulate.classe.td.on.tree}
\alias{simulate.classe.td.on.tree}
\title{Simulate an episodic ClaSSE candidate on a reconstructed tree}
\usage{
\method{simulate}{classe.td.on.tree}(
  backbone,
  pars,
  node.states = NULL,
  tip.states = NULL,
  k = NULL,
  state.labels = NULL,
  max.tries = 1000L,
  max.events = 100000L,
  max.taxa = 100000L,
  max.branch.tries = 100L,
  nsim = 1L,
  seed = NULL
)
}
\arguments{
\item{backbone}{A \code{classe_td_backbone} object, or a tree when \code{node.states}
and \code{tip.states} are supplied.}

\item{pars}{Named full parameter vector from \code{make.classe.td()}.}

\item{node.states, tip.states, k, state.labels}{Passed to
\code{\link[=mark.classe.td.backbone]{mark.classe.td.backbone()}} when \code{backbone} is a tree.}

\item{max.tries}{Maximum attempts to obtain a candidate that preserves all
supplied backbone lineages and endpoint states.}

\item{max.events, max.taxa}{Safety limits for each attempt.}

\item{max.branch.tries}{Maximum local rejection attempts for a pair of
observed daughter branches to satisfy their endpoint constraints.}

\item{nsim}{Number of independent stochastic maps to generate. The default
returns one \code{classe_td_tree_sim}; values above one return a list of such
objects with class \code{classe_td_tree_simulations}.}

\item{seed}{Optional random seed.}
}
\value{
A \code{classe_td_tree_sim} object whose \code{tree} element is a stochastic
mapping tree of class \code{simmap} and \code{phylo}, or for \code{nsim > 1}, a
\code{classe_td_tree_simulations} list containing those objects.
}
\description{
Simulates geographic transitions and hidden speciation events along a fixed
reconstructed backbone. Side trees are simulated without conditioning, so
the returned candidate may contain additional extant tips.
}
\details{
The supplied backbone consists of observed lineages, so their
survival is conditioned upon. Along each backbone branch, anagenetic and
hidden speciation events are simulated while unconditional extinction is
omitted for the continuing spine. At every hidden speciation, the second,
unobserved daughter is simulated with the full process and may either go
extinct or survive to the present.
}
\examples{
tree <- ape::read.tree(text = "(a:0.5,b:0.5);")
pars <- c(
  t.1 = 0.25,
  lambda111.1 = 0.8, mu1.1 = 2,
  lambda111.2 = 0.8, mu1.2 = 2
)
sim <- simulate.classe.td.on.tree(
  tree, pars, node.states = c(`3` = 1),
  tip.states = c(a = 1, b = 1), k = 1,
  state.labels = "A", seed = 3
)
sim$tree$maps
}
